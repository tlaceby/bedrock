// main.br

fn combine <T> (a: T, b: T) -> T {
  return a + b;
}

struct Circle {
  radius: number;

  static fn new (r: number) -> Circle {
    return Circle {
      radius: r,
    };
  }
}

const message = combine <string>("Hello ", "world");
// main.br

struct Circle {
    radius: number;
}

fn main () {

    let c1 = Circle{
        radius: 10.2,
    };

    // TODO: Work on struct instantiation and member expressions for structs.
    // Verify self.radius works inside the circle struct.

}

fn equal <T> (a: T, b: T) -> bool {
    return a == t;
}

equal <number> (10, 20);              // pass
// equal <string> ("Hello", "World"); // pass
// equal <string> ("Hello", 45);      // fail T cannot be both string and number
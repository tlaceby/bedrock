#include "../ast/ast_type.h"
#include "compiler.h"

using namespace ast;
using namespace runtime;
using namespace compiler;

void Compiler::compile_log_macro(LogMacro *m, shared_ptr<analysis::Scope> env) {
  //
}